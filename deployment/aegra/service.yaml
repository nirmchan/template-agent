apiVersion: v1
kind: Service
metadata:
  name: aegra-agent
  labels:
    app: aegra-agent
    component: langgraph-api
spec:
  type: ClusterIP
  ports:
    - port: 2024
      targetPort: 8123
      protocol: TCP
      name: http
  selector:
    app: aegra-agent
    component: langgraph-api
